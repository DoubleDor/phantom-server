# D3 PhantomServer example

This example uses PhantomeServer to render an SVG built using d3.

## Running the example

Run the following command, you should see an SVG printed to the console.

```
node index.js
```
